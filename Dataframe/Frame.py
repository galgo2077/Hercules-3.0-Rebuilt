"""Hercules Frame — central Polars DataFrame pipeline — Phase 4 implementation."""
import polars as pl


def build(ohlcv: pl.DataFrame) -> pl.DataFrame:
    """Build the Hercules Frame from raw OHLCV data."""
    raise NotImplementedError("Frame.build — Phase 4")

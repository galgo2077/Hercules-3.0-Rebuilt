# Backtest — simulation-specific settings. Does NOT alter strategy logic.
# Shared portfolio/strategy params come from Portfolio.toml + Strategy.toml.

[data]
start_date = "2026-04-01T00:00:00Z"
end_date   = "2026-07-20T00:00:00Z"
assets     = ["BTCUSDT", "ETHUSDT", "SOLUSDT", "XRPUSDT"]

[capital]
initial_cash        = 100.0
minimum_free_equity = 10.0
max_gross_exposure  = 1.0
allow_short         = true

[execution]
fee_rate      = 0.001
slippage_rate = 0.0005

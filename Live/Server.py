"""Live server — FastAPI app + dashboard — Phase 9 implementation."""
from SharedParams.Config import HerculesConfig


def main(config: HerculesConfig) -> None:
    raise NotImplementedError("Live.Server — Phase 9")
